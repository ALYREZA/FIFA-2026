# Cloudflare D1 (local Drizzle CLI only — not injected into the Worker)
CLOUDFLARE_ACCOUNT_ID=""
CLOUDFLARE_DATABASE_ID=""
CLOUDFLARE_D1_TOKEN=""

# Runtime secrets for pnpm preview live in .dev.vars — see .dev.vars.example
